#insertion sort
s = [8, 5, 3, 2, 1, 4, 6, 7]
lenS = len(s)
for j in range(1, lenS):
    key = s[j]
    i = j - 1
    while s[i] > key and i >= 0:
        s[i+1] = s[i]
        i = i-1
    s[i+1] = key

print s
        
